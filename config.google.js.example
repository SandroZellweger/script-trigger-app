/**
 * TEMPLATE FILE - Copy to config.private.js
 * 
 * This file shows the structure but should NOT contain real credentials.
 * Add your real Google API credentials to config.private.js instead.
 * 
 * Google Drive Picker Configuration Guide:
 * 
 * 1. Get API Key:
 *    - Go to: https://console.cloud.google.com/apis/credentials
 *    - Click "CREATE CREDENTIALS" → "API key"
 * 
 * 2. Get OAuth Client ID:
 *    - Same page, click "CREATE CREDENTIALS" → "OAuth client ID"
 *    - Type: Web application
 *    - Authorized JavaScript origins: http://localhost:8000, your domain
 *    - Format: 123456789-abc...xyz.apps.googleusercontent.com
 * 
 * 3. Extract App ID:
 *    - Take the first part before the hyphen from Client ID
 *    - Example: if Client ID is "123456789-abc.apps.googleusercontent.com"
 *    - App ID is: "123456789"
 */

// EXAMPLE STRUCTURE - DO NOT USE THESE VALUES
// Add real values to config.private.js

window.GOOGLE_CONFIG_EXAMPLE = {
    googleApiKey: 'YOUR_API_KEY_HERE',
    googleClientId: 'YOUR_CLIENT_ID.apps.googleusercontent.com',
    googleAppId: 'YOUR_APP_ID'
};
